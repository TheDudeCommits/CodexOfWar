# Unity Project Context

<!-- unity-onboarding:generated:start -->

## Project Summary

- Project root: `/Users/amir/Documents/Gauntlet Loop/game`
- Last analyzed: 2026-07-30
- Last analyzed commit: `aa395f556eb6863885d219437d48b719daa5a49f` plus uncommitted project scaffold

## Confirmed Environment

- Unity version: `6000.5.4f1` (`d550df8bd089`)
- Render pipeline: Universal Render Pipeline `17.5.0`; the active graphics
  asset is `Assets/Settings/PC_RPAsset.asset`
- Input system: Unity Input System `1.19.0`; active input handler is the new
  input system and `Assets/InputSystem_Actions.inputactions` is registered in
  Editor build settings
- Target platforms: native macOS/Apple Silicon is the first quality and review
  target; Windows and WebGL modules are installed but are not validated targets

## Important Packages And Frameworks

| Area | Finding | Confidence | Evidence |
| --- | --- | --- | --- |
| Rendering | URP `17.5.0`, Shader Graph via URP dependencies, PC and mobile pipeline assets | Confirmed | `Packages/manifest.json`, `Packages/packages-lock.json`, `ProjectSettings/GraphicsSettings.asset` |
| Input | Input System `1.19.0` with starter action asset | Confirmed | package manifest, `EditorBuildSettings.asset` |
| Navigation | AI Navigation `2.0.13` | Confirmed | package manifest |
| Testing | Unity Test Framework `1.7.0` | Confirmed | package manifest |
| Sequencing | Timeline `1.8.12` | Confirmed | package manifest |
| UI | uGUI `2.5.0`; no game UI architecture exists yet | Confirmed | package manifest, repository inspection |

## Directory Structure

| Path | Purpose | Confidence | Evidence |
| --- | --- | --- | --- |
| `Assets/Scenes` | Starter scene; will contain authored game and review scenes | Confirmed | filesystem and build settings |
| `Assets/Settings` | URP renderer, pipeline, and volume assets generated by the template | Confirmed | filesystem and graphics settings |
| `Assets/InputSystem_Actions.inputactions` | Template input actions | Confirmed | registered config object |
| `Packages` | Unity package manifest and resolved lock | Confirmed | filesystem |
| `ProjectSettings` | Project-wide Unity settings | Confirmed | filesystem |

## Assembly Boundaries

| Assembly | Responsibility | Key references | Notes |
| --- | --- | --- | --- |
| `Assembly-CSharp` | Template runtime assets only; no game runtime code yet | Unity runtime assemblies | A dedicated `CodexOfWar.Runtime` assembly is planned |
| `Assembly-CSharp-Editor` | Template tutorial editor script only | UnityEditor | A dedicated `CodexOfWar.Editor` assembly is planned |

## Scenes And Startup Flow

- Build scenes: `Assets/Scenes/SampleScene.unity`, enabled
- Likely startup scene: `Assets/Scenes/SampleScene.unity`
- Scene loading flow: none; the generated template starts directly in the
  sample scene

## Architecture

| Pattern | Finding | Confidence | Evidence |
| --- | --- | --- | --- |
| Existing game architecture | None; this is a clean URP template | Confirmed | full `Assets` inspection |
| Planned runtime | Plain-C# deterministic rules behind focused Unity adapters; explicit serialized composition; fixed 60 Hz gameplay clock | Planned | `Docs/Production/BuildPieces.md`, `ReviewProtocol.md` |
| Planned evidence | Editor-only deterministic scene/capture tools with runtime-free UnityEditor dependencies | Planned | P00 contract |

## Coding Conventions

- Namespace style: `CodexOfWar.<Domain>`
- Serialized fields: private fields with `[SerializeField]`; explicit required
  references and validation
- Async: no project convention yet; avoid mixing models until a feature needs
  asynchronous work
- Comments/docs: document intent, timing contracts, lifecycle constraints, and
  non-obvious review invariants

## Testing And Validation

- EditMode tests: no project tests yet; P00 will establish deterministic rule
  and manifest tests
- PlayMode tests: no project tests yet; runtime smoke coverage begins with the
  review scene
- CI/build validation: no repository CI yet; native macOS batch import and
  compilation are available locally

## Available Unity Tooling

| Capability | Status | Evidence |
| --- | --- | --- |
| `unity.connection.status` | unavailable | No Unity MCP server is installed in this task |
| `unity.editor.version` | repository/batch fallback | `ProjectVersion.txt`; Unity batch process |
| `unity.console.read` | batch-log fallback | `-logFile` output and project logs |
| `unity.scene.list` | repository fallback | serialized scene/build-settings inspection |
| `unity.scene.inspect` | repository fallback | serialized YAML inspection |
| `unity.buildsettings.read` | repository fallback | `EditorBuildSettings.asset` |
| `unity.gameobject.inspect` | repository fallback | serialized YAML inspection |
| `unity.asset.search` | repository fallback | filesystem and `rg` |
| `unity.package.read` | repository fallback | package manifest and lock |
| `unity.tests.list` | batch fallback once tests exist | Unity Test Framework is installed |
| `unity.tests.run` | batch fallback once tests exist | Unity `-runTests` |
| `unity.playmode.read` | unavailable | Requires Editor/UI automation or purpose-built runner |
| `unity.profiler.read` | unavailable | No structured Unity MCP profiler capability |

## Important Constraints

- The development Mac has Apple M2 graphics and 8 GB unified memory. URP is
  mandatory for this project; HDRP is intentionally excluded.
- Available disk space was approximately 17 GiB after the initial Unity import.
  Generated caches and builds must remain outside source control and be pruned
  deliberately.
- Unity MCP is not available. Serialized assets should be generated or modified
  through checked-in editor tooling and then imported by Unity batch mode,
  rather than hand-authoring fragile YAML.
- Benchmark screenshots are copyrighted local review inputs and must not be
  committed or deployed.
- Paid or restricted assets require a license manifest and local import step;
  source redistribution is prohibited unless the license explicitly permits it.

## Unknowns And Confidence

- Exact shipping platform set, signing/notarization requirements, and final
  minimum macOS version are not yet chosen.
- No runtime gameplay architecture, content license manifest, CI, or player
  build exists yet.
- First-time import emitted transient missing dependency warnings while URP
  package shaders were still importing; the clean post-import warning/error
  baseline must be captured in P00.

## Source Files Inspected

- `ProjectSettings/ProjectVersion.txt`
- `ProjectSettings/GraphicsSettings.asset`
- `ProjectSettings/ProjectSettings.asset`
- `ProjectSettings/EditorBuildSettings.asset`
- `Assets/Scenes/SampleScene.unity`
- `Assets/Settings/PC_RPAsset.asset`
- `Assets/Settings/PC_Renderer.asset`
- `Packages/manifest.json`
- `Packages/packages-lock.json`
- complete top-level `Assets` file inventory

<!-- unity-onboarding:generated:end -->
