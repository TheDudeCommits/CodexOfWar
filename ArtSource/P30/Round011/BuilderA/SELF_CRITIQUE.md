# P30 Round011 BuilderA self-critique

The smallest Round011 change closes the selected Round010 candidate's visible tick-34 standoff: an authored blade cutting-edge vertex and a posed target exterior triangle now resolve to the same world point, including in the identical-tick FX-off frame. The accepted tick-29 anticipation and tick-41 low grounded recoil pose bytes are locked unchanged by regression tests.

The lifecycle repair is deliberately orthogonal to combat mechanics. Pointer-lock requests are lifecycle-guarded and expected promise rejections are contained. Context restoration reconstructs postprocessing and PMREM state, reapplies renderer presentation state, refreshes viewport resources, and revalidates materials. Headed hardware probes returned zero errors, a 0.349% restore-luminance delta, and responsive movement in 68 ms.

The main visible limitation remains the existing high-emissive blade/bloom treatment around impact. The FX-off frame exposes the topology, but the production frame is still brighter and less materially restrained than the private reference bar. Round011 does not alter that frozen material/FX hierarchy.
